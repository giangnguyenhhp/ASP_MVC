@model IEnumerable<ASP_MVC.Models.Blog.Post>

@{
    ViewData["Title"] = "Danh sách Blog";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>@ViewData["Title"]</h1>
<partial name="_AlertMessage"/>
<p>
    <a class="btn btn-primary" asp-action="Create">Tạo Bài Viết Mới</a>
</p>
<p>Tổng số bài viết : @ViewBag.totalPosts</p>
<table class="table table-bordered">
    <thead>
    <tr class="">
        <th>
            STT
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Published)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Author)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateUpdated)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @(++ViewBag.postIndex)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PostId">
                    @Html.DisplayFor(modelItem => item.Title)
                </a>
                <br/>
                @if (item.Categories != null)
                {
                    <small>@string.Join(", ", item.Categories.Select(c => c.Title))</small>
                }
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Published)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Author.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateUpdated)
            </td>
            <td>
                <a class="btn btn-primary btn-sm" asp-action="Details" asp-route-id="@item.PostId">Chi tiết</a> |
                <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.PostId">Xóa</a>
            </td>
        </tr>
    }
    </tbody>
</table>

<partial name="_PagingXTL" model="@ViewBag.pagingModel"/>